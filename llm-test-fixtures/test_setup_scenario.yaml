name: test_setup_commands
description: "Test setup command execution and recording"
tier: 0
template_folder: qipu
tags: [setup, smoke]
target:
  binary: qipu
  env:
    QIPU_STORE: ".qipu"
task:
  prompt: "In the current directory, initialize qipu if needed. Create one note titled 'Setup Commands Note' that says the setup command test ran."
evaluation:
  gates:
    - type: command_output_contains
      command: qipu search "Setup Commands Note" --format records
      substring: "Setup Commands Note"
setup:
  commands:
    - "echo 'Setting up test environment'"
    - "pwd"
    - "ls -la"
