name: setup_test
description: "Test scenario with setup steps"
template_folder: qipu
tier: 1
task:
  prompt: "Create a new note about testing."
setup:
  commands:
    - "echo Running setup for test scenario"
evaluation:
  gates:
    - type: min_notes
      count: 1
